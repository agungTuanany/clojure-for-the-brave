;;; -*- Mode: clojure; -*-

(ns try-clojure-keys
     [:gen-class])

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))

(println "Cleanlines is next to goodlines")
